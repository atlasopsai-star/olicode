{
  "name": "olicode-design-benchmark-fixture",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "bun -e \"if (!(await Bun.file('index.html').exists())) process.exit(1)\""
  }
}
